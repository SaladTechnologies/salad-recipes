{
  "title": "NockPool GPU Prover",
  "description": "$replace",
  "type": "object",
  "required": [
    "container_group_name",
    "account_token"
  ],
  "properties": {
    "container_group_name": {
      "title": "Container Group Name",
      "description": "Required* Must be 2-63 lowercase letters, numbers, or hyphens. Cannot start with a number or start or end with a hyphen.",
      "type": "string",
      "maxLength": 63,
      "minLength": 2,
      "pattern": "^[a-z][a-z0-9-]{0,61}[a-z0-9]$"
    },
    "account_token": {
      "title": "NockPool Account Token",
      "description": "Required* Found on the NockPool Setup page after you sign in, connect your wallet, and generate an account token (starts with 'nockacct_').",
      "type": "string",
      "minLength": 8
    }
  }
}
