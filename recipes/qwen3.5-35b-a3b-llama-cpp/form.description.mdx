Run `Qwen3.5-35B-A3B` with `llama.cpp` on a Salad GPU as an OpenAI-compatible API for a wide variety of tools.

This recipe is designed to be easy for nontechnical users:

- the model is already chosen for you
- it downloads automatically on startup (might take about 20 minutes to start)
- you can use it from the built-in web UI or connect tools such as OpenClaw and OpenCode through its OpenAI-compatible API

If you want the simple version: enter a container group name, deploy, and wait for startup to finish.

If you want requests to require a SaladCloud API Key, enable Container Gateway Authentication in the deployment form.

Thinking is enabled by default. When it is enabled, you can control it per request by adding `/no_think` to disable reasoning for that turn or `/think` to explicitly enable it.

If you disable thinking in the form, the recipe sets Qwen's hard switch and `/think` will no longer override it.

If you want different llama.cpp settings later, you can still change the environment variables in Advanced Configuration after deployment.

Helpful references:

- [Unsloth Qwen3.5 model notes](https://unsloth.ai/docs/models/qwen3.5#qwen3.5-35b-a3b)
- [llama.cpp server docs](https://github.com/ggml-org/llama.cpp/tree/master/tools/server)
